apiVersion: apps/v1
kind: Deployment
metadata:
  name: voco-backend
  namespace: voco
spec:
  template:
    spec:
      containers:
        - name: backend
          envFrom:
            - configMapRef:
                name: voco-backend-env
            - secretRef:
                name: voco-secrets

