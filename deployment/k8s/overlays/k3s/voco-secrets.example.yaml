apiVersion: v1
kind: Secret
metadata:
  name: voco-secrets
  namespace: voco
type: Opaque
stringData:
  # Must match LiveKit `keys:` in livekit.yaml
  VOCO_LIVEKIT_API_KEY: devkey
  VOCO_LIVEKIT_API_SECRET: devsecret
