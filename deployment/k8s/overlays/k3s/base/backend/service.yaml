apiVersion: v1
kind: Service
metadata:
  name: voco-backend
  namespace: voco
spec:
  selector:
    app: voco-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP

