parent=keycloak.v2
import=common/keycloak

styles=css/voco.css
scripts=js/theme-sync.js js/otp-cells.js

# Disable Keycloak auto dark mode (prefers-color-scheme) — we control theme via cookie/CSS.
darkMode=false
